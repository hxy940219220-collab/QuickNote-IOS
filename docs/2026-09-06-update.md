# quicknote（ios） · 2026-09-06 更新

当前为 macOS 14+ 实现，名称标注不代表已有 iOS 构建目标。

- 同步桌宠、文件拖入、状态反馈、紧凑语音输入与本机离线复核。
- 同步字体统一、富文本/媒体间距、便签导航与编辑持久化改进。
- 修复 AI 设置自动触发钥匙串授权及读取失败后覆盖旧密钥库的风险；支持只重新配置当前接入。
- 文字翻译、解释、分析、拓展移除重复确认弹窗；只发送本次选中文字，发送范围和服务商信息在按钮悬停提示中可查看。
- 保留图片上传、整篇便签处理等其他确认机制。

模型权重通过校验下载脚本准备，不上传模型二进制、开发输出、用户便签或凭据。仓库包含桌宠运行素材、公开测试音频与相关许可证。

验证：独立仓库重新生成 Xcode 工程后，4 项定向检查通过（文字操作无重复确认、钥匙串拒绝保护、安全重新配置、公开中文音频离线解码）。另外核对生成的 Info.plist 保留图片识别系统服务与语音权限描述。
